Battle a la carte - 更新ログ
================================

記載ルール
----------
- このファイルは原則として日本語で記載する。
- 修正・改善を行うたびに更新する。
- 最新の記録を先頭に追記する。
- 各記録は「目的 / 変更点 / 範囲 / 確認」で簡潔に書く。

2026-04-14 (最新)
-----------------
目的:
- 更新ログを日本語中心に統一し、読みやすくする。

変更点:
- `UPDATE_LOG.txt` の全体構成と本文を日本語へ統一。
- 今後の運用ルール（日本語記載）を明文化。

範囲:
- ドキュメント（更新ログ）

確認:
- 見出し・項目名・本文が日本語で読めること。

2026-04-14 (前回)
-----------------
目的:
- スタート画面の使い勝手を改善する。
- ユーザー情報とコイン交換を分離する。

変更点:
- `web.html` / `mobile/mobile.html`
  - `コイン交換所` 用の独立ステージ（`start-coin-stage`）を追加。
  - `ユーザー情報` から交換UIを分離。
  - `推しスキル` 選択UIを追加。
  - `よく作る料理` 表示エリアを追加。
  - スキル選択に詳細表示エリア（`#start-skill-detail`）を追加。
- `main.js` / `mobile/main-sp.js`
  - `getPreferredStartSkillKey()` を追加し、対戦前の初期選択に反映。
  - コイン画面専用の表示・メッセージ処理を追加。
  - メニュー遷移を整理（`コイン交換所` と `ユーザー情報` を分離）。
  - スキル選択を 2列×3行の固定タイル表示に変更。
- `profile.js`
  - `favoriteSkillKey` をプロフィール保存項目に追加。
  - `recentDishes`（最近作った料理履歴）を追加。
  - `recordDishCooked(count, dishName)` で料理名を保存可能に変更。
- `player.js` / `mobile/player-sp.js`
  - 料理作成時に料理名を `recordDishCooked` へ渡すよう更新。
- `style.css` / `mobile/style-sp.css`
  - 2x3スキルタイル、スキル詳細欄、料理履歴表示のスタイル追加。
  - 戻るボタンを押しやすくするため、下部固定フッターを追加。
- キャッシュ対策
  - 参照バージョンを `20260414b` に更新。

範囲:
- Web版・モバイル版のスタート画面UIとプロフィール保存ロジック。

確認:
- ユーザー情報とコイン交換所が別画面で開くこと。
- 推しキャラ・推しスキルが次回セットアップの初期値に反映されること。
- スキル6種が1画面で選択しやすいこと。

2026-04-14 (前回)
-----------------
目的:
- 残っていた文字化けを解消する。

変更点:
- `web.html` / `mobile/mobile.html` の文字化け文言を修正。
  - 例: `確認すめ -> 確認する`, `捨てめ -> 捨てる`, `保孁 -> 保存` など。
- キャッシュ更新のため参照バージョンを `20260414a` に更新。

範囲:
- Web版・モバイル版HTML文言。

確認:
- 捨て札/山札確認ボタン、ユーザー情報保存まわりの文言が正常表示されること。

2026-04-13 (前回)
-----------------
目的:
- 対戦開始前の導線を分かりやすくする。
- スキル確認UIを追加する。

変更点:
- セットアップを 3段階に整理（キャラ -> スキル -> 先攻後攻）。
- スキル発動前に詳細確認できるパネルを追加。
- ルール表示にスキル一覧を追加。
- 一部HTMLの文字崩れとタグ崩れを修復。

範囲:
- Web版・モバイル版のスタート画面と対戦中スキルUI。

確認:
- 3段階遷移が正常に進むこと。
- スキル確認から発動できること。

2026-04-13 (前回)
-----------------
目的:
- HTMLの文字コード不整合で発生していた文字化けを修正。

変更点:
- `web.html` / `mobile/mobile.html` を UTF-8 前提で整備。
- 読めない表示を日本語として復旧。

範囲:
- Web版・モバイル版HTML。

確認:
- 主要UI文言が文字化けしないこと。

2026-04-13 (前回)
-----------------
目的:
- スキルシステムを試作実装し、CPU挙動と連携する。

変更点:
- スキル6種を実装（使用回数・条件・効果を管理）。
- CPU性格（バランス/妨害/逆転）とスキル優先ロジックを追加。
- 対戦HUDでスキル状態（未使用/使用済み/発動可）を表示。
- フレンド同期対象にスキル関連状態を追加。

範囲:
- 戦闘ロジック、UI、CPU挙動、同期データ。

確認:
- 1戦1スキル選択、回数制限、対象選択系スキルが機能すること。

2026-04-12 (前回)
-----------------
目的:
- Firestoreのセキュリティを参加者限定に強化する。

変更点:
- `firestore.rules` をホスト/ゲストUIDのみ読書き可の方針に更新。
- `FIREBASE_SETUP.md` に運用チェック手順を追記。

範囲:
- Firebase Firestore セキュリティ設定。

確認:
- 参加者以外がルーム文書へアクセスできないこと。

2026-04-12 (前回)
-----------------
目的:
- コイン交換所を本格運用できる状態にする。

変更点:
- 背景デザインのカタログ/購入/選択ロジックを追加。
- イベントカードイラスト背景を交換対象として追加。
- 設定画面とユーザー画面に交換UIを追加。

範囲:
- プロフィール、設定UI、背景描画。

確認:
- コイン消費、解放、選択反映が正常に動くこと。

2026-04-12 (前回)
-----------------
目的:
- Web版UIの視認性・重複表示を改善する。

変更点:
- 先攻後攻カードをモバイル同等の裏面デザインに統一。
- 料理作成ボタンの `!` 表示対応。
- 重複ログ表示を削除。
- 画面下の固定情報を整理し、フェイズ進行説明に置換。

範囲:
- Web版UI。

確認:
- ログ重複が解消され、進行説明が表示されること。

2026-04-12 (前回)
-----------------
目的:
- 手札が多い時の操作性を改善する。

変更点:
- 手札・セット・加工エリアに横ドラッグスクロールを追加。
- 候補料理が多い時の表示領域を拡張。
- 「やめる」ボタンを押しやすくするため固定表示を調整。

範囲:
- Web版・モバイル版の対戦画面UI。

確認:
- 大量手札でもカード選択しやすいこと。

2026-04-12 (前回)
-----------------
目的:
- Firebaseを使ったフレンド対戦の基盤を導入する。

変更点:
- `friend-battle-firebase.js` を追加。
- Web版/モバイル版にFirebase SDKと連携処理を組み込み。
- ルーム作成・参加の基本導線を追加。

範囲:
- フレンド対戦のリアルタイム同期基盤。

確認:
- Firebase設定済み環境で、合言葉ルームの作成/参加が行えること。

2026-04-14
-----------------
目的:
- スキル発動時のイベントカードコストを自動破棄ではなく、プレイヤーが選択できるようにする。

変更点:
- Web版/モバイル版で、イベントコストが必要なスキル発動時に「捨てるイベントカード選択」ステップを追加。
- 選択したイベントカードIDを使ってコスト処理するように変更（先頭カードの自動破棄を廃止）。
- コスト選択後に対象選択が必要なスキルはそのまま次ステップへ遷移するよう調整。

範囲:
- スキル発動フロー（Web版/モバイル版）。

確認:
- コストが必要なスキルで、任意のイベントカードを選んで発動できること。
- コストのみのスキル、コスト+対象選択スキルの両方で正常動作すること。

2026-04-14
-----------------
目的:
- 公式ホームページのXリンク先アカウントを変更する。

変更点:
- `index.html` のXリンクURLを `https://x.com/aniani_asobiba` に変更。
- 表示文言のアカウント名も `@aniani_asobiba` に変更。

範囲:
- 公式ホームページの外部リンク表示。

確認:
- ホームページのXボタンから新アカウントに遷移すること。

2026-04-14
-----------------
目的:
- スキル発動時にキャラクター別のカットイン演出を追加する。

変更点:
- 4キャラ分のスキル発動カットイン画像を `assets/images/skill-cutins/` に追加。
- Web版/モバイル版で、スキル発動時に専用スポットライト（2秒）を表示する処理を追加。
- 演出中の見た目を強化するため、スキル専用のオーバーレイ/カード演出（発光・シーン）を追加。
- GitHub Pagesのキャッシュ対策として `web.html` と `mobile/mobile.html` の読み込みバージョンを `20260414c` に更新。

範囲:
- スキル発動演出（Web版/モバイル版）、スポットライトCSS、静的画像アセット。

確認:
- プレイヤー/CPUどちらのスキル発動でも、選択キャラに応じたカットイン画像が表示されること。
- イベントカード演出と同じ表示時間（約2秒）で自動で消えること。

2026-04-14
-----------------
目的:
- 味見泥棒などイベントコスト選択付きスキルの確定不具合を修正し、Web版の手札調整案内を見やすくする。

変更点:
- スキルコスト選択で `costEventId` が数値IDでも正しく判定できるように修正（文字列限定判定を廃止）。
- コストID比較を型差異に強い比較へ変更し、選択後に確定が無反応になる問題を修正。
- Web版の「エンドフェイズ: あと◯枚捨ててください」を画面中央の強調表示に変更。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414d` に更新。

範囲:
- スキル発動フロー（Web版/モバイル版）、Web版の捨て札案内表示、HTMLバージョン文字列。

確認:
- 味見泥棒でイベントコストを選択→確定すると発動すること。
- Web版で手札調整が必要なとき、中央に案内が表示されること。

2026-04-14
-----------------
目的:
- Web版のリセットボタン重複を解消し、設定内リセットのみ残す。

変更点:
- `web.html` のヘッダー（ゲーム画面上部）にあった `ゲームリセット` ボタンを削除。
- `main.js` の `reset-game-button` 用イベント登録を削除。
- 設定オーバーレイ内の `settings-reset-button` はそのまま維持。
- Web版キャッシュ更新のため `web.html` の読み込みバージョンを `20260414e` に更新。

範囲:
- Web版ヘッダーUI、Web版イベントバインド、Web版HTMLバージョン文字列。

確認:
- ゲーム画面上部にリセットボタンが表示されないこと。
- 設定画面内のリセットボタンは引き続き利用できること。

2026-04-14
-----------------
目的:
- 4キャラの表情アイコンを新画像に統一し、既存アイコン参照を置き換える。

変更点:
- 新アイコン画像を `assets/images/character-icons/` に追加。
  - `chizuru-icons.png`
  - `mai-icons.png`
  - `takumi-icons.png`
  - `akatsuki-icons.png`
- Web版 `style.css` のキャラアイコン参照を新画像へ置き換え。
- モバイル版 `mobile/style-sp.css` のキャラアイコン参照を新画像へ置き換え。
- 表情切替（通常/笑顔/困り顔）は各画像の横3分割スプライトを使う方式で統一。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414f` に更新。

範囲:
- キャラクターアイコン表示（対戦HUD、開始画面、ギャラリーのキャラ表示）。

確認:
- Web/モバイルで4キャラとも新アイコンに切り替わっていること。
- スコア差で表情変化したときも正しい顔に切り替わること。

2026-04-14
-----------------
目的:
- 新キャラアイコンを全体的に約1.2倍に見えるよう拡大し、千鶴・舞依の表示位置を左上寄せに補正する。

変更点:
- Web版/モバイル版の4キャラアイコンスプライト表示倍率を `300%` → `360%` に変更（約1.2倍）。
- 千鶴・舞依の `background-position` を左上方向へ調整（右下寄り見えの補正）。
- 拡大設定は対戦HUD・開始画面・ギャラリーのキャラ表示にも反映。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414g` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、HTMLバージョン文字列。

確認:
- 4キャラの顔表示が以前より大きく表示されること。
- 千鶴・舞依が右下寄りではなく、中央寄りに見える位置へ補正されていること。

2026-04-14
-----------------
目的:
- 拡大後に見切れていたキャラ顔アイコンを中央寄りに再調整する。

変更点:
- Web版/モバイル版の4キャラ（通常・笑顔・困り顔）の `background-position` を再調整。
- 縦位置を `30%` へ下げ、顔が上で切れにくくなるよう補正。
- 千鶴・舞依は横位置オフセットも緩和し、中央寄りに補正。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414h` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、HTMLバージョン文字列。

確認:
- キャラ選択画面で顔が見切れず中央寄りに表示されること。
- 対戦中のHUDアイコンでも同様に自然な位置で表示されること。

2026-04-14
-----------------
目的:
- キャラアイコンがまだ右下寄りに見える問題を追加で補正し、顔位置を中央寄りにする。

変更点:
- Web版/モバイル版で4キャラの `background-position` を再調整。
- X方向は正方向オフセット（`+px`）に変更して中央寄りへ。
- Y方向は `30%` → `24%` に変更して上方向へ補正。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414i` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、HTMLバージョン文字列。

確認:
- キャラ選択画面で4キャラの顔が中央寄りに表示されること。
- 対戦HUDでも同様に右下寄りが解消されること。

2026-04-14
-----------------
目的:
- アイコン位置補正の方向ミスを修正し、左上寄り（中央寄り）に再調整する。

変更点:
- Web版/モバイル版の4キャラの `background-position` を再調整。
- X方向オフセットをさらに増やし（+12/+13/+8px）、右下寄りを解消。
- Y方向を `24%` → `36%` に変更し、上方向へ寄せるよう補正。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414j` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、HTMLバージョン文字列。

確認:
- キャラ選択画面で顔位置が右下寄りではなく中央寄りになること。
- 対戦HUDでも同様に顔の見切れが改善されること。

2026-04-15
-----------------
目的:
- キャラアイコンが右下寄りに見える問題を、スプライト拡大時のフレーム別補正で解消する。

変更点:
- Web版/モバイル版の4キャラアイコンについて、表情ごとにX補正を分離。
  - 通常: `calc(0% - 6px)`
  - 笑顔: `50%`
  - 困り顔: `calc(100% + 6px)`
- Y位置を `28%` に統一して、顔が中央に収まりやすい位置へ再調整。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260414k` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、HTMLバージョン文字列。

確認:
- キャラ選択画面で4キャラの顔が左右中央寄りになること。
- 表情切替時（通常/笑顔/困り顔）でも位置ズレが目立たないこと。
2026-04-15
-----------------
目的:
- キャラ選択時の顔アイコンがまだ右寄りに見えるため、全体をもう一段左寄せしつつ、わずかに拡大する。

変更点:
- Web版/モバイル版の4キャラアイコン（通常/笑顔/困り顔）の `background-size` を `380% auto` に統一（少しだけ拡大）。
- `background-position` を全体的に左へ再調整。
  - 通常: `calc(0% - 14px) 28%`
  - 笑顔: `calc(50% - 8px) 28%`
  - 困り顔: `calc(100% - 2px) 28%`
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260415a` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、`web.html`、`mobile/mobile.html`。

確認:
- CPU戦セットアップ画面の4キャラで、顔が以前より中央寄り（右寄り解消）に表示されること。
- 対戦中HUDでも同様に顔が見切れにくくなること。
2026-04-15
-----------------
目的:
- キャラ選択で千鶴・舞依だけをもう少し上に表示し、拓海・暁は現状維持する。

変更点:
- Web版/モバイル版で、千鶴・舞依の `background-position` のY値のみ `28%` → `24%` に調整。
- 拓海・暁は変更なし。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260415b` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、`web.html`、`mobile/mobile.html`。

確認:
- 千鶴・舞依だけが少し上に表示されること。
- 拓海・暁の表示位置は前回と同じであること。
2026-04-15
-----------------
目的:
- 千鶴・舞依の顔が下に埋まって見える問題を解消し、より中央に見える位置へ上方向補正する。

変更点:
- Web版/モバイル版で、千鶴・舞依のみ `background-position` のY値を `24%` → `38%` に変更。
- 拓海・暁は変更なし（前回設定を維持）。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260415c` に更新。

範囲:
- Web版 `style.css`、モバイル版 `mobile/style-sp.css`、`web.html`、`mobile/mobile.html`。

確認:
- 千鶴・舞依の顔が埋まらず、顔全体が中央寄りに見えること。
- 拓海・暁の表示位置は変化しないこと。
2026-04-15
-----------------
目的:
- 相手の選択スキルを事前に見えないようにし、読み合い性を上げる。
- 「味見泥棒」の発動条件を新仕様（自分0点・相手1点以上）へ変更する。

変更点:
- 相手スキルの表示を伏せるように変更。
  - 戦闘HUDの相手側スキル名を `スキル: ？？？` に固定。
  - 相手側スキル状態を `状態: 不明` に固定。
  - 開始時ログの相手スキル名表示を `相手「？？？」` に変更。
- 味見泥棒の仕様変更（Web/モバイル両対応）。
  - スキル定義の条件文を `自分の得点が0点で、相手が1点以上` に更新。
  - 実際の発動判定を `selfPlayer.score === 0 && enemyPlayer.score >= 1` に更新。
  - 非発動時メッセージを `条件: 自分0点かつ相手1点以上。` に更新。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260415d` に更新。

範囲:
- `state.js` / `mobile/state-sp.js`
- `player.js` / `mobile/player-sp.js`
- `render.js` / `mobile/render-sp.js`
- `main.js` / `mobile/main-sp.js`
- `web.html` / `mobile/mobile.html`

確認:
- 対戦中、相手のスキル名が事前に表示されないこと。
- 味見泥棒が「自分0点・相手1点以上」のときだけ発動可能になること。
2026-04-15
-----------------
目的:
- 暁・拓海のアイコンで首元がもう少し見えるよう、表示位置をわずかに上へ補正する。
- トップ画面のカード演出を控えめにし、1枚ずつ「5秒表示→フェードアウト」のサイクルにする。

変更点:
- アイコン位置（Web/モバイル共通）
  - 暁・拓海の `background-position` Y値を `28%` → `32%` に変更。
  - 千鶴・舞依は変更なし。
- トップ画面カード演出（Web/モバイル共通）
  - 表示カードを同時複数から「常に1枚のみ」に変更。
  - サイクルを `5秒表示 + 前後フェード`（合計約6.2秒）へ変更。
  - 生成間隔を `START_MENU_CARD_CYCLE_MS` に統一し、毎サイクルで前カードを消して次カードへ切替。
  - 回転/拡大ゆらぎを縮小、レア発光の頻度と強度を控えめ化。
  - フェードキーフレームを穏やかな推移へ調整（急激な明滅を抑制）。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260415e` に更新。

範囲:
- `style.css` / `mobile/style-sp.css`
- `main.js` / `mobile/main-sp.js`
- `web.html` / `mobile/mobile.html`

確認:
- 暁・拓海のアイコンで首元の見え方が少し改善されること。
- トップ画面でカードが1枚ずつ表示され、5秒程度の静止表示後にフェードアウトして次カードへ切り替わること。
2026-04-16
-----------------
目的:
- Battle à la carte Mode の専用素材（カットイン/アイコン）を実画像に差し替え、演出とUIの見た目を統一する。

変更点:
- Battle à la carte Mode 専用カットイン画像を追加。
  - `assets/images/battle-mode-cutins/chizuru-battle-mode-cutin.png`
  - `assets/images/battle-mode-cutins/mai-battle-mode-cutin.png`
  - `assets/images/battle-mode-cutins/takumi-battle-mode-cutin.png`
  - `assets/images/battle-mode-cutins/akatsuki-battle-mode-cutin.png`
- Battle à la carte Mode 発動時の演出参照先を、スキル用カットインから専用カットインへ分離。
  - `main.js` / `mobile/main-sp.js` に `BATTLE_MODE_CUTIN_IMAGE_PATHS` と専用参照関数を追加。
- Battle à la carte Mode 中のキャラアイコンを、シェフ版の顔中心アイコンへ差し替え。
  - 元画像から顔中心で切り抜き・透過処理した専用アイコンを追加。
  - `assets/images/battle-mode-icons/chizuru-battle-mode-icon.png`
  - `assets/images/battle-mode-icons/mai-battle-mode-icon.png`
  - `assets/images/battle-mode-icons/takumi-battle-mode-icon.png`
  - `assets/images/battle-mode-icons/akatsuki-battle-mode-icon.png`
  - Web/モバイルCSSで `battle-mode-chef` 時の背景画像を上記へ切り替え。
  - 旧「疑似コック帽（::before/::after）」演出は無効化。
- キャッシュ更新のため `web.html` / `mobile/mobile.html` の読み込みバージョンを `20260416a` に更新。

範囲:
- `main.js` / `mobile/main-sp.js`
- `style.css` / `mobile/style-sp.css`
- `web.html` / `mobile/mobile.html`
- `assets/images/battle-mode-cutins/*`
- `assets/images/battle-mode-icons/*`

確認:
- Battle à la carte Mode 発動時に、各キャラで専用カットイン画像が表示されること。
- Battle à la carte Mode 中、HUDのキャラアイコンがシェフ版の顔中心表示に切り替わること。
- Web版/モバイル版ともに差し替えが反映されること。
2026-04-16
-----------------
目的:
- ストーリーモード（チュートリアル）を追加し、会話パート → 対戦チュートリアル → 会話パートの流れで基本操作を学べるようにする。

変更点:
- トップメニューに `ストーリーモード（チュートリアル）` ボタンを追加（Web/モバイル）。
- ストーリー専用ステージを追加（会話画面・セリフ枠・アイコン枠・目標表示）。
  - 第1話: ルールレッスン（舞依×拓海）
    - 目標: セット / イベント発動 / 料理作成 / エンドフェイズ手札調整
    - 目標達成時に対戦を終了して会話パートへ戻る
  - 第2話: スキルレッスン（舞依×拓海）
    - スキル `切り札調達` を学習
    - 目標: `切り札調達` を発動 → 勝利
    - スキル発動前は料理作成・ターン終了をガイドでブロック
  - 第3話: `Battle à la carte Mode` は「後日実装予定」のプレースホルダ表示
- ストーリー対戦中の目標HUDを追加（Web/モバイル）。
  - 現在の目標を画面上に表示
  - 達成済み目標をハイライト
- 対戦終了時フックを追加し、ストーリー戦なら自動で会話パートへ復帰するよう対応。
- ストーリー進行をローカル保存（第1話/第2話クリアで次話解放）。
- `story-mode.js` を新規追加し、Web/モバイル共通で利用。

範囲:
- `web.html` / `mobile/mobile.html`
- `main.js` / `mobile/main-sp.js`
- `style.css` / `mobile/style-sp.css`
- `story-mode.js`（新規）

確認:
- メニューからストーリーモードへ遷移できること。
- 第1話で会話 → チュートリアル対戦 → 会話に戻る流れが動くこと。
- 第2話で「切り札調達」発動を先に学習し、その後勝利できること。
- 第3話が準備中表示になること。
- Web版/モバイル版の両方で同様に動くこと。

2026-04-16
-----------------
目的:
- トップ画面のモード選択順を見やすく調整する。

変更点:
- `ストーリーモード（チュートリアル）` の配置を、`料理開始（CPU戦）` と `料理開始（フレンド戦）` の間に変更（Web/モバイル）。

範囲:
- `web.html`
- `mobile/mobile.html`

2026-04-17
-----------------
目的:
- ストーリーモード第3話（Battle à la carte Mode学習回）を実装し、Modeの発動条件を調整する。

変更点:
- `story-mode.js` の第3話を「準備中」から実装済みに変更。
  - プレイヤー: 千鶴 / CPU: 暁
  - 会話パートを追加（ライバル同士の言い合い → 中盤再開）
  - 目標を追加:
    - 通常料理5品目を完成させて Mode 発動
    - Mode のまま対戦を最後まで完了
  - 中盤再開用の初期盤面を追加:
    - CPUが1点リード状態から開始
    - プレイヤーは通常料理4品済みで「あと1品でMode」状態
  - Mode発動時にチュートリアル説明ログを表示:
    - 発動条件（通常料理5品、緊急料理/創作料理は除外）
    - 効果（メインフェイズで1点料理完成時、1ターン1回追加ドロー）
  - 第3話クリア進行を保存対象に追加。

- Battle à la carte Mode の5品カウント条件を修正（Web/モバイル共通）。
  - `rules.js` / `mobile/rules-sp.js`:
    - 通常料理のみカウント
    - `緊急料理` / `創作料理`（イベント由来料理）はカウント対象外
  - ストーリー第3話中は CPU 側の Mode 発動を抑止し、プレイヤー体験を優先。

- ルール説明文を更新（Web/モバイル共通）。
  - `render.js` / `mobile/render-sp.js`:
    - 「通常料理5個でMode」
    - 「緊急料理/創作料理はカウント外」
- キャッシュ残り対策として、読み込みバージョンを `20260417a` に更新。
  - `web.html` / `mobile/mobile.html`

範囲:
- `story-mode.js`
- `rules.js`
- `mobile/rules-sp.js`
- `render.js`
- `mobile/render-sp.js`
- `web.html`
- `mobile/mobile.html`

2026-04-17
-----------------
目的:
- Battle à la carte Mode用アイコンで、線画が透ける問題を修正する。

変更点:
- `assets/images/battle-mode-icons` 内の4キャラアイコンPNGをアルファ補正。
  - 対象:
    - `chizuru-battle-mode-icon.png`
    - `mai-battle-mode-icon.png`
    - `takumi-battle-mode-icon.png`
    - `akatsuki-battle-mode-icon.png`
  - 背景透過は維持しつつ、線画や本体の半透明を強化して透けを軽減。
  - 極端な薄い半透明（A1-191）を実質解消し、視認性を改善。
- 元画像バックアップを作成:
  - `assets/images/battle-mode-icons/_backup-20260417-alpha-fix`

確認:
- Battle à la carte Mode時のキャラアイコンで、顔や線画が背景に透けにくくなっていること。
- Web版/モバイル版の両方で同じアイコンが反映されること。

2026-04-17
-----------------
目的:
- Battle à la carte Mode の効果不足を補完し、演出と視認性を強化する。

変更点:
- Battle à la carte Mode の新効果を追加（Web/モバイル共通）。
  - ドローフェイズ後、毎ターン1回だけ捨て札から材料カード1枚を回収可能。
  - プレイヤー側は選択UIで回収対象を指定。
  - CPU/フレンド側は自動で回収。
  - 実装:
    - `state.js` / `mobile/state-sp.js`
    - `rules.js` / `mobile/rules-sp.js`
    - `player.js` / `mobile/player-sp.js`
    - `cpu.js` / `mobile/cpu-sp.js`
    - `friend-battle-firebase.js`
    - `story-mode.js`（初期化フラグの追加）

- Battle à la carte Mode カットイン演出を強化（Web/モバイル共通）。
  - 専用クラス `battle-mode` を追加し、表示時間を延長（3.4秒）。
  - 専用エフェクト（背景パルス、光彩、強調アニメーション）を追加。
  - 決め台詞を反映:
    - `潜在能力覚醒！`
    - `すべての食材に感謝！バトルアラカルトモード！！`
  - 実装:
    - `main.js` / `mobile/main-sp.js`
    - `style.css` / `mobile/style-sp.css`

- Battle à la carte Mode アイコンの雰囲気を「神々しい」方向へ強化（Web/モバイル共通）。
  - 光彩・オーラ・脈動アニメーションを追加。
  - 既存アイコン画像を置換せず、CSS演出で強化。
  - 実装:
    - `style.css` / `mobile/style-sp.css`

- ルール表示へ新効果文を追記（Web/モバイル共通）。
  - `render.js` / `mobile/render-sp.js`

- キャッシュ更新。
  - `web.html` / `mobile/mobile.html`
  - `v=20260417a` → `v=20260417b`

補足:
- この環境では `git` / `node` コマンドが利用できず、実機実行テストは未実施。
- 反映確認はブラウザのハードリロード（キャッシュ削除）で確認推奨。

2026-04-17
-----------------
目的:
- シェフアイコンを新画像へ差し替え、Battle à la carte Mode の演出テンポを指定どおりに調整する。

変更点:
- シェフアイコン画像を新規添付画像へ差し替え（Web/モバイル共通で同一参照先）。
  - 置換先:
    - `assets/images/battle-mode-icons/chizuru-battle-mode-icon.png`
    - `assets/images/battle-mode-icons/mai-battle-mode-icon.png`
    - `assets/images/battle-mode-icons/takumi-battle-mode-icon.png`
    - `assets/images/battle-mode-icons/akatsuki-battle-mode-icon.png`

- 不要画像を削除。
  - `assets/images/battle-mode-icons/_backup-20260417-alpha-fix`（旧バックアップ）
  - ルート直下の差し替え元一時画像:
    - `千鶴シェフ.png`
    - `舞依シェフ.png`
    - `拓海シェフ.png`
    - `暁シェフ.png`

- Battle à la carte Mode カットインを3段階構成へ変更（Web/モバイル共通）。
  - 1段目（文字のみ）: `潜在覚醒！` を 1秒
  - 2段目（文字のみ）: `すべての食材に感謝！` を 2秒
  - 3段目（既存カットイン画像）: `バトルアラカルトモード！！` で 3秒
  - 実装:
    - `main.js`
    - `mobile/main-sp.js`
  - 文字のみ表示用スタイルを追加:
    - `style.css`
    - `mobile/style-sp.css`

- Battle Mode中の神々しいオーラを「少し控えめ」に調整。
  - キャラアイコンの発光強度・拡散・スケール脈動を弱化。
  - 雰囲気は維持しつつ、視認性とのバランスを改善。
  - 対象:
    - `style.css`
    - `mobile/style-sp.css`

- キャッシュ更新。
  - `web.html` / `mobile/mobile.html`
  - `v=20260417b` → `v=20260417c`

2026-04-17
-----------------
目的:
- Battle à la carte Mode中のシェフ姿アイコンが遠く見える問題を改善し、顔の視認性を上げる。

変更点:
- シェフ姿アイコンの拡大率を大幅に引き上げ（Web/モバイル共通）。
  - 千鶴/舞依: `134%` → `196%`
  - 拓海: `140%` → `206%`
  - 暁: `138%` → `202%`

- 顔が見えやすくなるように表示位置を上寄せ調整（Web/モバイル共通）。
  - 千鶴/舞依: `50% 18%` → `49% 9%`
  - 拓海: `50% 17%` → `50% 8%`
  - 暁: `50% 17%` → `50% 8%`

- 対象ファイル:
  - `style.css`
  - `mobile/style-sp.css`

2026-04-18
-----------------
目的:
- 画像反映のラグ（表示が遅い・切り替えが重い）を、原因優先で改善する。

原因（優先度高）:
- `createImageCard` 内で `new Image()` を都度生成しており、同じ画像を再描画のたびに再確認していた。
- `updateUI()` が短時間に何度も呼ばれ、DOM更新が過密になっていた（全体再描画の連打）。
- 初回表示時に必要画像が未ロードで、表示タイミングで読み込み待ちが発生していた。

対応:
- 画像ロードキャッシュを追加（Web/モバイル）。
  - 画像パスごとに `loading / loaded / error` を共有管理。
  - 同一画像の `new Image()` 多重生成を廃止。
  - `createImageCard` はキャッシュ状態を参照して描画し、エラー時のみフォールバック。
  - 対象:
    - `render.js`
    - `mobile/render-sp.js`

- 起動後の段階的プリロードを追加（Web/モバイル）。
  - 手札で使う材料・イベント・加工アイテム画像を `requestIdleCallback`（未対応環境は `setTimeout`）で少しずつ先読み。
  - 一気に読み込まず、2枚ずつ分割して負荷を平準化。
  - 対象:
    - `render.js`
    - `mobile/render-sp.js`

- UI更新をフレーム単位で集約（Web/モバイル）。
  - `updateUI()` 連打時、同フレーム内の重複更新をキュー化して1回に集約。
  - 即時反映が必要な場合は `updateUIImmediate()` を追加して呼べるようにした。
  - 対象:
    - `render.js`
    - `mobile/render-sp.js`

- 重い領域のDOM再生成を抑制（Web/モバイル）。
  - 手札・セット・加工アイテム・最新料理・リアルタイムログに「シグネチャ比較」を導入。
  - 内容が同じときは `innerHTML` の全消去/再構築をスキップ。
  - 対象:
    - `render.js`
    - `mobile/render-sp.js`

- 先読み対象を拡張（Web/モバイル）。
  - 材料/イベント/料理/加工アイテムに加えて、スキルカットイン・Battle Modeカットインも段階先読み。
  - 2枚ずつの分割読み込みで、瞬間負荷を抑えながら待ち時間を軽減。
  - 対象:
    - `render.js`
    - `mobile/render-sp.js`

- キャッシュバージョン更新。
  - `web.html` / `mobile/mobile.html`
  - `v=20260417c/d` 系 → `v=20260418a`

補足:
- 画像ファイル自体の圧縮（再エンコード）は、この実行環境に画像圧縮ツールが無いため未実施。
- まずは「再読込の無駄」と「再描画の過密」を解消して、体感速度を優先して改善。

2026-04-18
-----------------
目的:
- Battle à la carte Modeアイコンをさらに拡大し、顔が中央に見えるよう再調整する。

変更点:
- シェフ姿アイコンのズームをさらに強化（Web/モバイル共通）。
  - 千鶴/舞依: `196%` → `252%`
  - 拓海: `206%` → `262%`
  - 暁: `202%` → `258%`

- 顔中心へ寄せるため表示位置を中央寄りへ調整（Web/モバイル共通）。
  - 千鶴/舞依: `49% 9%` → `50% 14%`
  - 拓海/暁: `50% 8%` → `50% 12%`

- 対象:
  - `style.css`
  - `mobile/style-sp.css`

- キャッシュバージョン更新:
  - `web.html` / `mobile/mobile.html`
  - `v=20260418a` → `v=20260418b`

2026-04-18
-----------------
目的:
- 起動時の文字化けを修正する。

原因:
- `web.html` と `mobile/mobile.html` の保存文字コードが Shift-JIS になっており、
  ファイル内宣言の `UTF-8` と不一致になっていた。

対応:
- `web.html` / `mobile/mobile.html` を UTF-8 へ変換。
- UTF-8 デコード検証を実施し、日本語文字列（例: 「ターン: プレイヤー」「料理」「設定」）が正常であることを確認。

補足:
- PowerShell の既定表示では UTF-8 ファイルが文字化けして見える場合があるため、
  検証は UTF-8 デコードで実施。

2026-04-18
-----------------
目的:
- Web版の対戦画面を「一画面固定」にし、ブラウザの縦スクロールなしで表示できるようにする。

対応:
- Web版のみ、対戦開始後（スタートオーバーレイ非表示時）にビューポート自動フィットを適用。
  - 画面サイズに合わせて `#game-container` の表示倍率を自動計算し、全体を1画面に収める。
  - リサイズ時も再計算して追従。
  - 対象: `main.js`
    - `applyWebViewportFit`
    - `scheduleWebViewportFit`
    - `setupWebViewportFitOnce`

- CSS側でWeb対戦時のスクロール抑止と縮尺適用を追加。
  - `body.web-battle-fixed` 時にブラウザスクロールを無効化。
  - `#game-container` に `transform: scale(var(--web-fit-scale))` を適用。
  - 対象: `style.css`

- キャッシュ更新（Web版のみ）。
  - `web.html`
  - `v=20260418b` → `v=20260418c`

2026-04-18
-----------------
目的:
- シェフ姿（Battle à la carte Mode）アイコンをさらに拡大し、顔がはっきり見えるようにする。

変更点:
- Battle Modeアイコンの拡大率を再調整（Web/モバイル共通）。
  - 千鶴/舞依: `252%` → `308%`
  - 拓海: `262%` → `320%`
  - 暁: `258%` → `316%`

- 顔位置を中央寄りへ調整（Web/モバイル共通）。
  - 千鶴/舞依: `50% 14%` → `50% 19%`
  - 拓海/暁: `50% 12%` → `50% 18%`

- 対象:
  - `style.css`
  - `mobile/style-sp.css`

- キャッシュバージョン更新:
  - `web.html` / `mobile/mobile.html`
  - `v=20260418b/c` → `v=20260418e`

2026-04-18
-----------------
目的:
- 公式ホームページの `note` リンクを正しいURLに修正し、スキル/バトルアラカルトの説明を追加する。

変更点:
- `index.html`
  - ヘッダーナビに `システム`（`#systems`）を追加。
  - 新セクション `スキル & Battle à la carte Mode` を追加。
    - スキルの概要（選択制、条件発動、使用状態表示）を掲載。
    - Battle à la carte Mode の概要（発動条件・継続効果）を掲載。
    - それぞれカットイン画像を表示。
  - `あにあに note` リンクを以下へ修正:
    - `https://note.com/aniani_asobiba`

- `home.css`
  - 新セクション用スタイルを追加:
    - `.mechanics-grid`
    - `.mechanic-card`
    - `.mechanic-media`
    - `.mechanic-body`
  - レスポンシブ時の表示を調整（1024px/768px以下）。

- キャッシュ対策:
  - `index.html` の読み込みCSSを `home.css?v=20260418f` に更新。

2026-04-18
-----------------
目的:
- ホームページの `note` リンク再修正と、カード紹介の表示枚数統一。

変更点:
- `index.html`
  - `あにあに note` リンクを再修正:
    - `https://note.com/gentle_rhino6093`
  - 材料カード表示を 5枚 → 4枚 に調整（イベント/料理カードと同数に統一）。

2026-04-18
-----------------
目的:
- ゲーム画面が崩れて起動不能になるバグを緊急復旧。

原因:
- `web.html` / `mobile/mobile.html` 内の多数の閉じタグが崩れており、
  DOM構造が壊れていたため画面挙動が不安定化していた。

対応:
- `web.html` / `mobile/mobile.html` の壊れた閉じタグを一括復旧。
  - `E/button>` などを `</button>` 形式へ修正。
- 文字化け修復時の副作用で壊れていた `title` タグを修正。
  - `<titl"> ... </titl">` → `<title> ... </title>`
- 開閉タグ数の整合性を確認（button/div/section/h3）。

2026-04-18
-----------------
目的:
- Web版/モバイル版に残っていた文字化けを再スキャンして修正する。

対応:
- `web.html` / `mobile/mobile.html` の文字化け文言を再修正。
  - 例: `設定` / `イベント` / `未選択` / `スキル発動` / `ターン終了` / `ストーリーモード` など
  - スタートメニュー、CPU戦セットアップ、フレンド戦、ユーザー情報、確認ダイアログ類を修正
- モバイル版のルール説明（簡易/詳細/特殊勝利）を全文差し替えで正常化。
- 修正後、文字化けパターン（`�`, `セチE`, `E��` など）を全ファイル横断検索し、0件を確認。

キャッシュ対策:
- `web.html` / `mobile/mobile.html` のCSS/JS読込クエリを更新
  - `v=20260418e` → `v=20260418g`

2026-04-18
-----------------
目的:
- シェフモード（Battle à la carte Mode）アイコンを丸型表示にし、顔アップで見やすくする。

変更点:
- `style.css` / `mobile/style-sp.css`
  - `.character-icon.battle-mode-chef` に丸型クリップを適用。
    - `border-radius: 50%`
    - うっすら円形枠を追加（神々しさ演出は維持）
  - シェフモード時の顔アップ倍率を強化。
    - 千鶴/舞依: `308%` → `368%`（`background-position: 50% 16%`）
    - 拓海/暁: `320%/316%` → `384%`（`background-position: 50% 15%`）

キャッシュ対策:
- `web.html` / `mobile/mobile.html` の読込バージョン更新
  - `v=20260418g` → `v=20260418h`

2026-04-18
-----------------
目的:
- シェフモードアイコンの発光エフェクトによる「ぼやけ」を解消する。

変更点:
- `style.css` / `mobile/style-sp.css`
  - `.character-icon.battle-mode-chef` のぼかし要因を停止
    - `filter: none`
    - `animation: none`
    - `overflow: hidden`（円形マスク内に収める）
  - 疑似要素エフェクトを停止
    - `::before`, `::after` を `content: none; display: none;` に変更
  - 代替として軽い固定シャドウのみ付与（輪郭を見やすくする目的）

キャッシュ対策:
- `web.html` / `mobile/mobile.html` の読込バージョン更新
  - `v=20260418h` → `v=20260418i`

2026-04-19
-----------------
目的:
- フォルダ内にあるBGM（`assets/audio/bgm.mp3`）を、対戦開始時点で確実に再生する。

変更点:
- `main.js`
  - `onTurnCardSelected()` で、先攻/後攻カード選択直後に
    - `safeStartGame()`
    - `unlockAudio()`
    - `startBgmOnce()`
    を実行するよう修正。
  - `beginMatchByRole()` 冒頭にも `unlockAudio()` + `startBgmOnce()` を追加し、開始時の取りこぼしを防止。
  - `window.__battleStartBgmOnce` を公開し、他モードからも同じBGM開始処理を呼べるよう統一。

- `mobile/main-sp.js`
  - Web版と同じ修正を適用。
  - 対戦開始導線（先攻/後攻カード選択）でBGMが起動するよう統一。
  - `window.__battleStartBgmOnce` を公開。

- `story-mode.js`
  - `setupStoryBattle()` で `window.__battleStartBgmOnce()` を呼び、チュートリアル対戦でもBGM開始。

- `friend-battle-firebase.js`
  - `ensureRuntimeStarted()` で `window.__battleStartBgmOnce()` を呼び、フレンド戦開始時のBGM起動を追加。

キャッシュ対策:
- `web.html` / `mobile/mobile.html` のCSS/JS読込クエリを更新
  - `v=20260418i` → `v=20260419a`

2026-04-19
-----------------
目的:
- Web版で「エンドフェイズの捨て札案内」が捨てる操作の邪魔になる問題を解消。
- BGMが鳴らない/小さい問題を再調整。

変更点:
- `style.css`
  - Web版 `discard-banner` を中央固定から上部配置へ変更。
    - `position: fixed` → `position: absolute`
    - `top: 76px`（Web固定表示時は `top: 84px`）
    - サイズを少し縮小し、パルスアニメーションを停止。
  - これにより、手札選択や「捨てる」ボタン付近と重ならない配置へ調整。

- `audio.js` / `mobile/audio-sp.js`
  - BGM初期音量を微増。
    - `0.35` → `0.45`
  - `playBGM()` を調整し、再生中に毎回先頭へ戻さないよう修正（再生安定化）。

- `main.js` / `mobile/main-sp.js`
  - `startBgmOnce()` を再調整。
    - 初回のみ BGMをONにして開始。
    - 2回目以降も `playBGM()` を呼び、再生失敗後の再試行を可能化。
  - スタートメニューの主要導線（CPU/ストーリー/フレンド）クリック時に `unlockAudio()` を先行実行し、ブラウザ制限でBGM開始が失敗しにくいよう改善。

キャッシュ対策:
- `web.html` / `mobile/mobile.html` の読込クエリを更新
  - `v=20260419a` → `v=20260419b`

2026-04-19
-----------------
目的:
- BGMを場面ごとに追加し、タイトル/ストーリー会話/バトルアラカルト/対戦終了後で切り替わるようにする。
- 対戦中BGMの選択肢を増やし、設定画面から変更できるようにする。

追加した音源（`assets/audio`）:
- `battle-mode.mp3`（バトルアラカルトモード）
- `story-dialogue.mp3`（ストーリー会話）
- `title-screen.mp3`（タイトル/メニュー）
- `match-result.mp3`（対戦終了後）
- `bgm-miracle.mp3`（対戦中BGM候補）
- `bgm-sky-high-refrain.mp3`（対戦中BGM候補）
- `bgm-code241-2.mp3`（対戦中BGM候補）

変更点:
- `audio.js` / `mobile/audio-sp.js`
  - BGM管理を拡張し、場面別再生APIを追加:
    - `playTitleBGM()`
    - `playStoryBGM()`
    - `playBattleModeBGM()`
    - `playResultBGM()`
  - 対戦中BGMの選択肢を4曲化:
    - `Normal` / `Miracle` / `Sky High Refrain` / `Code.241_2`
  - 旧キー `variantA` を互換エイリアスとして吸収（既存設定の読み込み対策）。
  - BGMは「現在の場面」1系統のみ再生されるよう統一（切替時に他を停止）。

- `main.js` / `mobile/main-sp.js`
  - `showStartStage()` で `start-title-stage` / `start-menu-stage` 表示時に `playTitleBGM()` を呼ぶよう追加。
  - `endGame()` で `playResultBGM()` を呼ぶよう変更（未対応環境のみ `stopBGM()` にフォールバック）。
  - 初回タップ時に `unlockAudio()` + `playTitleBGM()` を実行する導線を追加（ブラウザの自動再生制限対策）。

- `rules.js` / `mobile/rules-sp.js`
  - Battle à la carte Mode 発動時に `playBattleModeBGM()` を呼ぶよう追加。

- `story-mode.js`
  - ストーリー画面（選択/導入会話/終了会話）で `playStoryBGM()` を呼ぶよう追加。

- `render.js` / `mobile/render-sp.js`
  - `applyRuntimeSettings()` でBGMトラック設定適用後に `getCurrentBgmTrack()` で正規化キーへ同期。

キャッシュ対策:
- `web.html` / `mobile/mobile.html` の読込クエリを更新
  - `v=20260419b` → `v=20260419c`

2026-04-19
-----------------
目的:
- 設定画面にBGM音量スライダーを追加する。
- Battle à la carte Mode発動後、対戦終了まで専用BGMを継続再生する。
- 背景デザインの見え方を約10%濃く調整する。
- 設定画面のBGM名表示を数字にする。
- ストーリー第3話の玉ねぎ不足感と、カード総枚数不変の要件を満たす。

変更点:
- `audio.js` / `mobile/audio-sp.js`
  - 対戦BGMのラベルを `1` / `2` / `3` / `4` に変更。
  - BGM音量APIを追加:
    - `setBgmVolume()`
    - `getBgmVolume()`
  - Battle à la carte Mode用のBGMロックを追加:
    - `setBattleModeBgmLocked(true)` 中は通常対戦BGMへ戻らず、専用BGMを維持。
    - `endGame()` 側でロック解除して結果BGMへ遷移。
  - ループ音源ごとのベース音量を整理し、スライダー連動で全BGMに反映。

- `render.js` / `mobile/render-sp.js`
  - 設定の `bgmVolume` を読み書きする処理を追加。
  - 設定オーバーレイのサウンド欄に音量スライダーを追加。
  - 入力中はリアルタイム反映、確定時にログ出力するよう調整。

- `state.js` / `mobile/state-sp.js`
  - 設定の初期値に `bgmVolume: 0.8` を追加。

- `main.js` / `mobile/main-sp.js`
  - ゲーム開始時にBattle Mode BGMロックを明示的に解除。
  - `endGame()` でロック解除後に結果BGMを再生。

- `rules.js` / `mobile/rules-sp.js`
  - Battle à la carte Mode突入時にBGMロックを有効化。

- `story-mode.js`
  - `takeCard()` の「不足時に新規カード生成」を廃止。
    - これにより、ストーリー初期化で総枚数が増える挙動を防止。
  - 第3話のプレイヤー初期手札に `たまねぎ` を追加。

- `style.css` / `mobile/style-sp.css`
  - 背景デザインの透過率を調整（約10%濃く表示）。
  - 設定用のスライダー表示スタイルを追加。

キャッシュ対策:
- `web.html` / `mobile/mobile.html` の読込クエリを更新
  - `v=20260419c` → `v=20260419d`
